import { Text } from "@mantine/core";

export default function MicrosoftView() {
    return (
        <Text align="center" size={15} mt={20}>
            If you are seeing this text, you have encountered an error. Please refresh the page. A modal should display instead.
        </Text>
    );
}
