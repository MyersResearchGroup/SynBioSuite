Flask
flask_cors
requests
packaging==21.3
# uploadFlapjack.py imports `flapjack`, which comes from pyflapjack. excel2flapjack
# is never imported anywhere in this backend -- it is a leftover from the older
# Flapjack path that uploadFlapjack.py replaced. Dropping it also removes a
# dependency conflict: excel2flapjack 1.1.1 requires pandas>=2.0.0, while both
# excel2sbol and pyflapjack pin pandas==1.5.3.
pyflapjack==1.1.0
# Pinned to the exact commit this backend was built and tested against.
# PyPI has no 1.1.19: its releases go 1.1.18 -> 2.0.0 -> 2.0.1, and 2.0.x is a
# different major version that has never converted these workbooks. Revisit by
# validating 2.0.1 properly -- a branch pin is fragile if the branch moves.
excel2sbol @ git+https://github.com/SynBioDex/Excel-to-SBOL@5910698
sbol2build
flask_swagger_ui
xlsxwriter
pandas
openpyxl
pudupy @ git+https://github.com/MyersResearchGroup/PUDU
